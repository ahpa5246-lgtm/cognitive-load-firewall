const emergencyPatterns=[/worsening severe headache/i,/repeated vomiting/i,/seizure/i,/loss of consciousness/i,/increasing confusion/i,/increasing agitation/i,/unable to wake/i,/weakness/i,/numbness/i,/slurred speech/i,/severe deterioration/i,/significant deterioration/i];
const boundaryPattern=/\b(return to|play|football|drive|driving|fully recovered|how severe|take medication|medication)\b/i;
export function safetyScan(input:string){const matches=emergencyPatterns.filter(pattern=>pattern.test(input));const requiresCare=boundaryPattern.test(input);return{blocked:matches.length>0,requiresCare,matchCount:matches.length,message:matches.length?"This tool cannot determine severity. Seek urgent medical evaluation or local emergency services.":requiresCare?"This tool cannot diagnose, prescribe, or clear activity. Please ask a qualified clinician for guidance.":null};}
