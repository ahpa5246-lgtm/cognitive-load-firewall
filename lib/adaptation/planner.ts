import type { AdaptationPlan,LoadVector } from "@/lib/types";
export function buildAdaptationPlan(mismatch:LoadVector):AdaptationPlan{const strategies:AdaptationPlan["strategies"]=[];if(mismatch.reading>15)strategies.push({type:"CHUNK_CONTENT",reason:"readingMismatch",priority:1});if(mismatch.density>10)strategies.push({type:"REDUCE_INFORMATION_DENSITY",reason:"densityMismatch",priority:2});if(mismatch.memory>10)strategies.push({type:"ADD_MEMORY_SUPPORT",reason:"memoryMismatch",priority:3});if(mismatch.motion>5)strategies.push({type:"REDUCE_MOTION",reason:"motionMismatch",priority:4});if(mismatch.attention>15)strategies.push({type:"GUIDED_STEPS",reason:"attentionMismatch",priority:5});return{strategies};}
