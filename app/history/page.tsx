"use client";
import { ActionLink, PageIntro, Shell } from "@/app/ui";
import { useEffect, useState } from "react";
export default function HistoryPage() { const [items, setItems] = useState<string[]>([]); useEffect(() => { const raw = localStorage.getItem("clf-history"); if (raw) setItems(JSON.parse(raw)); }, []); function clear() { localStorage.removeItem("clf-history"); setItems([]); } return <Shell><PageIntro eyebrow="History" title="Return without starting over.">Your guest history is stored locally in this browser. It contains titles and modes, not a recovery score.</PageIntro>{items.length ? <section className="history-list">{items.map((item, i) => <article className="panel" key={`${item}-${i}`}><span className="label">Adaptation {i + 1}</span><h2>{item}</h2><ActionLink href="/demo" secondary>Reopen demo</ActionLink></article>)}<button className="text-button" onClick={clear}>Delete adaptation history</button></section> : <section className="panel empty"><h2>Nothing here yet.</h2><p>Complete an adaptation and it can appear here for this guest session.</p><ActionLink href="/demo">Adapt content</ActionLink></section>}</Shell>; }
