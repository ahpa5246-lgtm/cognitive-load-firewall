"use client";
import { useState } from "react";
import { Shell } from "@/app/ui";
export default function AccessibilityPage() { const [large, setLarge] = useState(false); const [contrast, setContrast] = useState(false); return <Shell><section className="narrow"><div className="eyebrow">Accessibility lab</div><h1>Calm is a behavior, not a color.</h1><p className="lede">Try the controls below. The preview uses semantic controls, visible focus, readable sizing, and no color-only status.</p><div className="support-grid">{["Keyboard navigation", "Visible focus states", "Reduced motion", "Text resizing", "Screen reader landmarks", "No status by color alone", "Optional read aloud", "High contrast surfaces"].map(item => <div className="support-item" key={item}><span aria-hidden="true">✓</span><strong>{item}</strong></div>)}</div><section className={`panel preview ${large ? "large-preview" : ""} ${contrast ? "high-contrast" : ""}`}><span className="label">Interactive preview</span><h2>One task at a time</h2><p>Use these controls and move through the preview with Tab.</p><div className="actions"><button className="secondary" onClick={() => setLarge(!large)} aria-pressed={large}>Larger text</button><button className="secondary" onClick={() => setContrast(!contrast)} aria-pressed={contrast}>High contrast</button></div><button className="primary" onClick={() => window.speechSynthesis?.speak(new SpeechSynthesisUtterance("One task at a time."))}>Read preview aloud</button></section></section></Shell>; }