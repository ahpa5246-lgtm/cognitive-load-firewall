import Link from "next/link";

export default function AccessibilityPage() {
  return <main className="app-shell"><header className="topbar"><Link className="brand" href="/">Cognitive Load <span>Firewall</span></Link><Link href="/demo">Open demo →</Link></header><section className="narrow"><div className="eyebrow">Accessibility lab</div><h1>Calm is a behavior, not a color.</h1><p className="lede">The product reduces unnecessary processing demands while keeping controls discoverable and content available.</p><div className="support-grid">{["Keyboard navigation", "Visible focus states", "Reduced motion", "Text resizing", "Screen reader landmarks", "No status by color alone", "Optional read aloud", "High contrast surfaces"].map((item) => <div className="support-item" key={item}><span aria-hidden="true">✓</span><strong>{item}</strong></div>)}</div><div className="panel preview"><span className="label">Interactive preview</span><h2>One task at a time</h2><p>Try moving through this control group with Tab. Focus remains visible, and the page honors your reduced-motion preference.</p><div className="segmented"><button className="selected">Standard</button><button>Calm</button></div></div></section></main>;
}